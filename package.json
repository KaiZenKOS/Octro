{
  "name": "octro",
  "private": true,
  "type": "module",
  "description": "Octro monorepo — moteur de coordination financiere et integration XRPL Track 1 Loaded.",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "npm run build && npm run test --workspaces --if-present",
    "ci": "npm run typecheck && npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^5.0.0"
  }
}

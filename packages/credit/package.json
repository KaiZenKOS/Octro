{
  "name": "@octro/credit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Moteur de scoring credit (methodologie Odoo Credit Assessment Report, TypeScript pur, sans I/O) — Phase D.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^5.0.0"
  }
}

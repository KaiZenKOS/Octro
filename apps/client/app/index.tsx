import { AccountScreen } from '../src/account-screens';
// Arrivee sur le site = le parcours reel (compte -> KYC simule -> credit
// Odoo -> lending), plus une maquette de demonstration. L'ancienne page
// d'accueil fixture (Lina) reste disponible sur /demo.
export default function Home() { return <AccountScreen />; }

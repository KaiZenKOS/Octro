import { LendingScreen } from '../src/account-screens';
// Depot/retrait (lender) et emprunt a montant et duree precis (borrower)
// sur le vault ouvert partage.
export default function Lending() { return <LendingScreen />; }

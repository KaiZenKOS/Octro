import { TransactionsScreen } from '../src/account-screens';
// Adresse complete, soldes reels (XRP + RLUSD simule) et historique de
// transactions on-chain du wallet de l'utilisateur.
export default function Transactions() { return <TransactionsScreen />; }

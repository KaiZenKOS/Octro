import { InfoScreen } from '../src/info-screen';
// Page publique (pas de garde d'authentification) : track, saveur
// (vanilla/loaded) et source officielle du hackathon.
export default function Info() { return <InfoScreen />; }
